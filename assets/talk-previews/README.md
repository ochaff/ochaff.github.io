# Preview images

`placeholder.svg` is used for talk and code cards that do not yet have an output image.

To replace a preview, add an image in this folder and update the corresponding `src` in `index.md`.
